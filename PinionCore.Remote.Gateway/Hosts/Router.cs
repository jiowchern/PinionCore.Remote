using System;
using System.Collections.Generic;
using System.Text;
using PinionCore.Remote.Gateway.Protocols;

namespace PinionCore.Remote.Gateway.Hosts
{
    internal class Router
    {
        internal void Join(ISession session)
        {
            throw new NotImplementedException();
        }

        internal void Leave(ISession session)
        {
            throw new NotImplementedException();
        }

        internal void Register(uint group, IUserService service1)
        {
            throw new NotImplementedException();
        }

        internal void Unregister(IUserService service1)
        {
            throw new NotImplementedException();
        }
    }
}
